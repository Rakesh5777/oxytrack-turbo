{
  "name": "tenent-client",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "swagger-cli bundle ./src/spec.yaml --outfile ./dist/spec.yaml --type yaml",
    "generate-api-client": "swagger-cli bundle ./src/spec.yaml --outfile ./dist/spec.yaml --type yaml & docker run --rm -v \"%cd%:/local\" -v \"%cd%:/tenent_client\" openapitools/openapi-generator-cli generate -i /local/dist/spec.yaml -g typescript-axios -o /tenent_client"
  },
  "dependencies": {
    "swagger-cli": "^4.0.4"
  }
}